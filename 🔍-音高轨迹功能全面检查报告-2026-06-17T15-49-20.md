---

## 🔍 音高轨迹功能全面检查报告

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    PitchTraceScreen                      │
│  (pitch_canvas.dart / recordings_sheet.dart / settings)  │
└────────────────────────┬────────────────────────────────┘
                         │ watch / read
┌────────────────────────▼────────────────────────────────┐
│               PitchTraceController                       │
│  (状态管理 / 设置持久化 / 生命周期 / 录音管理)              │
└───────────┬────────────────────────────┬─────────────────┘
            │                            │
┌───────────▼──────────┐    ┌────────────▼────────────────┐
│  PitchTraceService   │    │      DatabaseService        │
│  (MethodChannel /    │    │   (设置 / 录音元数据)         │
│   EventChannel)      │    └─────────────────────────────┘
└───────────┬──────────┘
            │
┌───────────▼──────────────────────────────────────────────┐
│              Android 原生 PitchTracker                    │
│  (AudioRecord / YIN 算法 / WAV 录音 / MediaPlayer 回放)   │
└──────────────────────────────────────────────────────────┘
```

---

### 一、Model 层检查 ✅ 良好

| 组件 | 评估 | 详情 |
|------|------|------|
| `PitchReading` | ✅ 正确 | MIDI 换算公式正确（`69 + 12 * log2(f/440)`），cents 偏差计算准确（`1200 * log2(f/target)`） |
| `PitchTraceRecording` | ✅ 正确 | 不可变数据类，`copyWith` 支持部分更新 |
| `MusicalScale` | ✅ 正确 | 大调/小调音程定义正确，`noteIndices()` 返回正确音名集合 |

**无问题。**

---

### 二、Service 层检查 ✅ 良好

| 检查项 | 评估 | 详情 |
|--------|------|------|
| MethodChannel 通信 | ✅ 正确 | `start`/`stop`/`pause`/`resume`/`seek` 方法签名完整 |
| EventChannel 数据流 | ✅ 正确 | `readings` 使用 `receiveBroadcastStream()` + `where`/`map` 过滤转换 |
| 错误处理 | ✅ 正确 | `PlatformException` → `PitchTraceException` 转换，用户友好消息 |
| 录音文件管理 | ✅ 正确 | `listRecordings`/`deleteRecording`/`playRecording`/`stopPlayback` 完整 |
| 暂停/恢复 | ✅ 正确 | `pauseRecording`/`resumeRecording` 支持录音过程暂停 |

**无问题。**

---

### 三、Controller 层检查 ⚠️ 有 2 个潜在风险

| 检查项 | 评估 | 详情 |
|--------|------|------|
| 实时数据接收 | ✅ 正确 | `_listenToReadings()` 订阅 EventChannel 流，自动取消旧订阅 |
| 历史数据管理 | ✅ 合理 | 5 分钟时间窗口移除，`removeWhere` O(n) 但 n 受窗口限制（最多 ~18000 条） |
| 设置持久化 | ✅ 正确 | 250ms debounce 避免频繁写入，`_scheduleSettingsSave()` 实现正确 |
| 生命周期管理 | ✅ 正确 | `AppLifecycleObserver` 集成，后台自动停止/前台恢复 |
| 录音元数据 | ✅ 正确 | JSON 序列化到 `app_settings`，启动时加载，修改时保存 |
| 回放控制 | ✅ 正确 | 播放/暂停/跳转/停止完整实现，`_startPlaybackTimer` 定位更新 |
| 错误处理 | ✅ 正确 | 37 处 try-catch，`PitchTraceException` 统一处理 |
| notifyListeners 频率 | ⚠️ 潜在风险 | 每次音高读数（~60Hz）都调用 `notifyListeners()`，可能在低端设备上造成 UI 卡顿 |
| 内存边界 | ⚠️ 潜在风险 | `_history` 和 `_loadedRecordingHistory` 无硬性容量上限，仅靠时间窗口控制 |

**`notifyListeners` 调用统计**：30 处调用点，其中 `_listenToReadings` 中每帧调用一次（~60Hz）。

**建议**：
1. 考虑对 `notifyListeners` 做节流（如 30Hz = 33ms 间隔），减少 UI 重建频率
2. 为 `_history` 添加硬性容量上限（如 20000 条）作为安全网

---

### 四、Screen 层检查 ✅ 良好

| 检查项 | 评估 | 详情 |
|--------|------|------|
| 画布渲染 | ✅ 正确 | `CustomPaint` + `PitchCanvasPainter` 实现，支持缩放/滚动 |
| 音高点颜色 | ✅ 正确 | 根据 cents 偏差分 5 档颜色（准/微偏/偏/严重偏/极偏） |
| 网格系统 | ✅ 正确 | 半音网格线 + 音名标签 + 时间网格，音阶内音符高亮 |
| 录音叠加显示 | ✅ 正确 | `_loadedRecordingHistory` 独立于实时历史，支持查看历史录音轨迹 |
| 回放控制栏 | ✅ 正确 | `RecordingPlaybackBar` 显示播放进度，支持暂停/跳转/停止 |
| 录音列表 | ✅ 正确 | `RecordingsListSheet` 使用 `DraggableScrollableSheet`，支持重命名/备注/删除 |
| 设置页面 | ✅ 正确 | 参考音 A4、频率范围、可见时长、MIDI 跨度、音阶过滤、中心音 |
| 空状态处理 | ✅ 正确 | 无录音时显示提示文字和图标 |
| 中文输入 | ✅ 正确 | 重命名/备注使用 `TextEditScreen` 独立页面，避免 Dialog 输入问题 |

**无问题。**

---

### 五、Android 原生端检查 ✅ 良好

| 检查项 | 评估 | 详情 |
|--------|------|------|
| 音频采集 | ✅ 正确 | `AudioRecord` 44100Hz 单声道 16bit，窗口 4096 采样点 |
| 音高检测算法 | ✅ 正确 | YIN 风格自相关算法，阈值 0.18，支持 parabolic interpolation |
| 频率范围限制 | ✅ 正确 | `minLag`/`maxLag` 根据频率范围计算，避免检测超出范围的频率 |
| 振幅阈值 | ✅ 正确 | 低于 0.006 时返回无音高，避免静音段误检 |
| 线程安全 | ✅ 正确 | `@Volatile` 标记共享变量，`worker` 线程独立运行 |
| WAV 录音 | ✅ 正确 | 标准 WAV 格式写入，44100Hz 16bit 单声道 |
| 暂停/恢复 | ✅ 正确 | `pause()` 停止录音并释放资源，`resume()` 重新初始化 |
| 音高历史保存 | ✅ 正确 | 录音停止时将音高轨迹保存为 JSON 文件（与 WAV 同名） |
| 回放 | ✅ 正确 | 使用 `MediaPlayer` 播放 WAV 文件 |
| 权限处理 | ✅ 正正确 | 运行时请求 `RECORD_AUDIO` 权限 |

**无问题。**

---

### 六、数据流完整性检查 ✅ 正确

```
实时模式：
  Android AudioRecord → PitchTracker.analyze() → EventChannel
  → PitchTraceService.readings → PitchTraceController._listenToReadings()
  → _history.add() + notifyListeners() → PitchCanvas 实时绘制

录音保存：
  PitchTracker.stop() → writeWav() + writePitchHistory()
  → 返回 WAV 路径 → Controller 保存到 latestRecordingPath

录音回放：
  Controller.playRecording(path) → Service.playRecording(path)
  → Android MediaPlayer → 同时加载 .json 历史 → 画布叠加显示

设置持久化：
  Controller.setXxx() → _scheduleSettingsSave() (250ms debounce)
  → DatabaseService.setSetting() → SQLite app_settings 表
```

---

### 七、发现的问题汇总

| # | 严重程度 | 问题 | 影响 | 建议修复 |
|---|----------|------|------|----------|
| 1 | ⚠️ 低 | `notifyListeners()` 以 ~60Hz 频率调用 | 低端设备可能 UI 卡顿 | 添加 33ms 节流 |
| 2 | ⚠️ 低 | `_history` 无硬性容量上限 | 极端情况下内存增长 | 添加 20000 条上限 |
| 3 | ℹ️ 信息 | `_loadedRecordingHistory` 从 JSON 文件同步加载 | 大录音文件可能加载慢 | 当前可接受 |

**结论**：音高轨迹功能整体实现质量**良好**，架构清晰、算法正确、错误处理完善。仅有 2 个低优先级的性能优化点，不影响正常使用。

---

需要我修复这两个低优先级问题吗？
