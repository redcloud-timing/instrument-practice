根据对代码的深入分析，我来为您详细介绍这个长笛练习App的**音高轨迹功能**：

## 📊 功能概述

音高轨迹是这个App的核心功能之一，主要用于**实时检测长笛演奏的音高**，并将音高变化以可视化轨迹的形式展示出来，帮助练习者监控音准。

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────┐
│                    Flutter UI 层                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ pitch_trace  │  │ pitch_canvas │  │ recordings   │  │
│  │ _screen.dart │  │   .dart      │  │ _sheet.dart  │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                 │           │
│  ┌──────┴─────────────────┴─────────────────┴───────┐  │
│  │          PitchTraceController (状态管理)          │  │
│  └───────────────────────┬───────────────────────────┘  │
│                          │                              │
│  ┌───────────────────────┴───────────────────────────┐  │
│  │           PitchTraceService (平台通道)             │  │
│  └───────────────────────┬───────────────────────────┘  │
└──────────────────────────┼──────────────────────────────┘
                           │ MethodChannel / EventChannel
┌──────────────────────────┼──────────────────────────────┐
│                    Android 原生层                        │
│  ┌───────────────────────┴───────────────────────────┐  │
│  │              MainActivity.kt                      │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ AudioRecord │  │  YIN 算法   │  │ WAV 录音  │ │  │
│  │  │  (麦克风)   │  │ (音高检测)  │  │           │ │  │
│  │  └─────────────┘  └─────────────┘  └───────────┘ │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 核心组件详解

### 1. **PitchTraceController** (`lib/controllers/pitch_trace_controller.dart`)
这是音高轨迹的核心控制器，负责：
- **状态管理**：管理检测状态、录音状态、播放状态
- **设置持久化**：保存用户的频率范围、音阶过滤等设置
- **录音管理**：创建、暂停、恢复、停止录音
- **回放控制**：播放/暂停/跳转到指定位置

**关键参数**：
```dart
defaultMinFrequency = 80.0 Hz    // 最低检测频率
defaultMaxFrequency = 2200.0 Hz  // 最高检测频率
defaultVisibleDurationMs = 8000  // 默认显示时长（毫秒）
defaultMidiSpan = 24.0           // 默认MIDI音高跨度
```

### 2. **PitchTraceService** (`lib/services/pitch_trace_service.dart`)
平台通信服务，通过两个通道与原生层交互：
- **MethodChannel** (`flute_practice/pitch_trace`)：发送控制命令（启动/停止/录音等）
- **EventChannel** (`flute_practice/pitch_trace_events`)：接收实时音高数据流

### 3. **PitchReading** (`lib/models/pitch_reading.dart`)
音高读数数据模型，包含：
- `frequency`：检测到的频率（Hz）
- `amplitude`：振幅
- `clarity`：清晰度（0-1）
- `timestampMillis`：时间戳

**核心方法**：
- `midiNumberFor(referenceA4Hz)`：将频率转换为MIDI音符编号
- `centsFor(referenceA4Hz)`：计算音分偏差（±50音分）
- `noteNameFor(referenceA4Hz)`：获取音名（如 C4, D#5）

### 4. **MusicalScale** (`lib/models/musical_scale.dart`)
音阶定义，用于过滤显示：
- 支持**大调**和**小调**
- 可选择根音（C, C#, D, ... B）
- 用于在轨迹上高亮显示属于当前音阶的音符

---

## 🎯 核心功能

### 1. **实时音高检测**
- 使用 **YIN 算法**（一种自相关音高检测算法）
- 采样率：通常 44100 Hz
- 窗口大小：2048 样本
- 实时通过 EventChannel 推送音高数据

### 2. **音高轨迹可视化**
在 `pitch_canvas.dart` 中实现：
- **时间轴**：水平方向，显示时间流逝
- **音高轴**：垂直方向，显示MIDI音符
- **网格线**：
  - 根音线（突出显示）
  - 属于当前音阶的音符线
  - 其他音符线
- **音高点**：
  - 根据清晰度和音准程度着色
  - 绿色：音准良好
  - 黄色：轻微偏差
  - 红色：偏差较大
- **方向指示**：显示音高上升/下降/平稳

### 3. **录音功能**
- **开始录音**：同时开始音高检测和WAV录制
- **暂停/恢复**：支持中断后继续
- **停止录音**：保存为WAV文件
- **元数据**：支持为录音添加标题和备注

### 4. **回放功能**
- 播放已录制的音高轨迹
- 显示播放位置线
- 支持跳转到指定时间点
- 同步显示该时刻的音高数据

### 5. **设置选项**
在 `pitch_trace_settings_screen.dart` 中：
- **频率范围**：调整检测的最低/最高频率
- **可见时长**：调整轨迹窗口显示的时间长度（3秒-30秒）
- **MIDI跨度**：调整垂直方向显示的音高范围
- **音阶过滤**：选择当前练习的音阶
- **参考音高**：调整A4基准频率（默认440Hz）

---

## 📱 Android 原生实现

在 `MainActivity.kt` 中：

### 音频录制
```kotlin
// 使用 AudioRecord 进行实时音频采集
val recorder = AudioRecord(
    MediaRecorder.AudioSource.MIC,
    sampleRate,
    AudioFormat.CHANNEL_IN_MONO,
    AudioFormat.ENCODING_PCM_16BIT,
    recordBufferSize
)
```

### YIN 音高检测算法
```kotlin
// YIN 算法核心步骤：
1. 计算差分函数（difference function）
2. 累积均值归一化（cumulative mean normalized difference）
3. 绝对阈值检测（absolute threshold = 0.18）
4. 抛物线插值（parabolic interpolation）提高精度
```

### WAV 录音
- 录制为标准 WAV 格式
- 存储在应用私有目录 `filesDir/recordings/`
- 支持暂停/恢复录制

---

## 🎨 UI 界面

### 主界面 (`pitch_trace_screen.dart`)
- **顶部工具栏**：设置、录音列表
- **中央画布**：实时音高轨迹
- **底部控制栏**：
  - 开始/停止检测
  - 录音按钮
  - 时间显示
  - 当前音高信息

### 录音列表 (`recordings_sheet.dart`)
- 显示所有历史录音
- 支持播放、删除
- 显示录音时长、标题、备注

### 设置界面 (`pitch_trace_settings_screen.dart`)
- 频率范围滑块
- 可见时长选择
- MIDI跨度选择
- 音阶选择器
- 参考音高调整

---

## 💡 使用场景

1. **音准练习**：实时查看音高偏差，调整吹奏力度和口型
2. **长音练习**：保持音高稳定，观察轨迹平直度
3. **音阶练习**：在音阶过滤模式下，只显示目标音符
4. **录音回放**：录制练习过程，事后分析问题
5. **进度追踪**：通过历史录音对比练习进步

---

## ⚙️ 技术特点

1. **低延迟**：使用原生音频API，延迟控制在毫秒级
2. **高精度**：YIN算法可检测到±1音分的偏差
3. **流畅渲染**：使用CustomPainter进行高性能绘制
4. **状态持久化**：所有设置自动保存到SQLite
5. **后台处理**：支持应用切后台时自动暂停

这是一个功能完整、实现专业的音高检测系统，非常适合长笛练习者使用！
